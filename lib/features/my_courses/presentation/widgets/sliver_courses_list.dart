import 'package:flutter/material.dart';

class SliverCoursesList extends StatelessWidget {
  const SliverCoursesList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
